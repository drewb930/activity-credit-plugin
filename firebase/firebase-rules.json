{
	"rules": {
		"$workspace": {
			".read": "auth.workspaces[$workspace] != null",
			".write": "auth.workspaces[$workspace] !== null && (auth.workspaces[$workspace] === 'owner' || auth.workspaces[$workspace] === 'admin' || auth.workspaces[$workspace] === 'server')"
		}
	}
}
